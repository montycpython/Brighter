name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android Platforms
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager "platforms;android-36" "platforms;android-35" "build-tools;35.0.0" "platform-tools" || true

      - name: Prepare Keystore, Config & Environment
        run: |
          touch .env
          touch .env.example
          # Generate fresh debug keystore
          rm -f debug.keystore
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          
          # Create google-services.json matching applicationId com.aistudio.bwriter.xmkty
          cat << 'EOF' > app/google-services.json
          {
            "project_info": {
              "project_number": "123456789012",
              "project_id": "bwriter-app",
              "storage_bucket": "bwriter-app.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:123456789012:android:abcdef0123456789",
                  "android_client_info": {
                    "package_name": "com.aistudio.bwriter.xmkty"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "AIzaSyDummyKeyForBuildOnly123456789"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          EOF

          # Setup Gradle 9.3.1 wrapper
          gradle wrapper --gradle-version 9.3.1 || true
          chmod +x ./gradlew || true

      - name: Assemble Debug APK
        run: |
          ./gradlew :app:assembleDebug \
            --no-configuration-cache \
            --no-build-cache \
            --no-daemon \
            -x test \
            -x lint \
            -x verifyRoborazziDebug \
            --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Bwriter-Debug-APK
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
